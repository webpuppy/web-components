import { css } from '../../../globals/exports';

export default css`
:host {
	display: block;
}

.wp-ftr {
	background-color: var(--wp-navy);
	color: rgb(255, 255, 255);
	display: block;
	margin: 0 auto;
	max-width: 100%;
	padding: .25em;
}
`;
