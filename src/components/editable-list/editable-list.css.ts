import { css } from 'lit-exports';

export default css`
:host {
	display: block;
	line-height: var(--wp-content-line);
}

.wp-editable-list {
	border-radius: var(--wp-border-radius);
	line-height: var(--wp-content-line);
	padding: 2rem .85rem;
}
`;
