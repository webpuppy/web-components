import { css } from '../../globals/exports';

export const DEFAULT_IMG_STYLES = css`
:host {
	display: inline-block;
	padding: .25rem;
}

.wp-img {
	margin: 0 auto;
}

.wp-img.wp-img--md {
	height: 128px;
	width: 128px;
}
`;
